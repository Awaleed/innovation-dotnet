<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <RootNamespace>Innovation.KeycloakTheme</RootNamespace>
    <IsPublishable>false</IsPublishable>
    <IsPackable>false</IsPackable>
    <IsAspireProjectResource>false</IsAspireProjectResource>
    <!-- Prevent dotnet from trying to compile TypeScript/React files -->
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

  <ItemGroup>
    <Content Remove="**" />
    <None Include="package.json;package-lock.json;vite.config.ts;tsconfig.json" />
    <None Include="src\**" Exclude="node_modules\**" />
  </ItemGroup>

  <!-- Debug: ensure node_modules installed and build theme JAR if missing -->
  <Target Name="DebugEnsureNodeEnv" BeforeTargets="Build" Condition=" '$(Configuration)' == 'Debug' ">
    <Exec Command="node --version" ContinueOnError="true">
      <Output TaskParameter="ExitCode" PropertyName="ErrorCode" />
    </Exec>
    <Error Condition="'$(ErrorCode)' != '0'" Text="Node.js is required to build the Keycloak theme." />
    <!-- Install deps if missing -->
    <Exec Command="npm install" Condition=" !Exists('node_modules') " />
    <!-- Build JAR if missing -->
    <Message Importance="high" Text="Building Keycloak theme JAR..." Condition=" !Exists('dist_keycloak\keycloak-theme-for-kc-all-other-versions.jar') " />
    <Exec Command="npm run build" Condition=" !Exists('dist_keycloak\keycloak-theme-for-kc-all-other-versions.jar') " />
    <Exec Command="npx keycloakify build" Condition=" !Exists('dist_keycloak\keycloak-theme-for-kc-all-other-versions.jar') " />
  </Target>

  <!-- Publish: always rebuild Keycloak theme JAR -->
  <Target Name="BuildKeycloakTheme" AfterTargets="ComputeFilesToPublish">
    <Message Importance="high" Text="Building Keycloak theme JAR..." />
    <Exec Command="npm install" />
    <Exec Command="npm run build" />
    <Exec Command="npx keycloakify build" />
    <ItemGroup>
      <ThemeJars Include="dist_keycloak\*.jar" />
      <ResolvedFileToPublish Include="@(ThemeJars->'%(FullPath)')" Exclude="@(ResolvedFileToPublish)">
        <RelativePath>keycloak-theme\%(FileName)%(Extension)</RelativePath>
        <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      </ResolvedFileToPublish>
    </ItemGroup>
  </Target>

</Project>
